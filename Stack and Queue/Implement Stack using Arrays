#include <bits/stdc++.h>
using namespace std;
int arr[100];
int Top = -1;
void push(int n){
	Top++;
	arr[Top] = n;
}
void pop(){
	Top--;
}
int top(){
	return arr[Top];
}
int size(){
	return Top + 1;
}
bool empty(){
	if(Top == -1) return 1;
	return 0;
}
int main() {
    int n;
    cin>>n;
    int k;
    for(int i=0;i<n;i++){
    	cin>>k;
    	push(k);
    }
    cout<<size()<<endl;
    for(int i=0;i<n;i++){
    	cout<<top()<<" ";
    	pop();
    }
    cout<<endl;
    cout<<size()<<endl;
    if(empty()){
    	cout<<"empty"<<endl;
    }
    else{
    	cout<<"Not empty"<<endl;
    }
	return 0;
}
